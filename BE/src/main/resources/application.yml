spring:
  application:
    name: BE

  profiles:
    active: local

  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  config:
    import: optional:file:.env[.properties]